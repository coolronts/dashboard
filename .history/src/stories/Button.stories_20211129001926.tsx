import { Primary, RoundIcon } from "../Components/Buttons";

export default {
  title: "Buttons",
  component: Primary,
  RoundIcon,
};

export const PrimaryButton = () => <Primary name="Primary" />;
