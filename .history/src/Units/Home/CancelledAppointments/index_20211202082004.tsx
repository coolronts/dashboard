import BarDashBoard from "../../../Components/BarDashBoard";

const CancelledAppointments: React.FC = () => {
  return (
    <div>
      <BarDashBoard size="lg" barWidth={270} />
    </div>
  );
};

export default CancelledAppointments;
